# Tools

## Generate Mock Scan

Create a sample WebSocket-style JSON scan payload:

```bash
python tools/generate_mock_scan.py > samples/sample_scan.json
```

The generated payload can be pasted into browser console tests or used as reference data for dashboard development.
